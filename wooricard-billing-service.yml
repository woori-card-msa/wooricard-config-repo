spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/billing_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul

server:
  port: 8083

logging:
  level:
    com.card.payment: DEBUG

# 승인/결제 서비스 설정
approval:
  service:
    url: http://wooricard-approval-service # TODO: 주소 동적 변수로 변경 

